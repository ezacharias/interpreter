-- Throw(tag : Tag⟦a, b⟧)(x : a) : b
-- Catch(tag : Tag⟦a, b⟧, f : () ⟶ c)(g : a ⟶ (b ⟶ c) ⟶ c) ⟶ c

sum Bool
  False
  True

new T : Tag⟦Bool, ()⟧

Main() : Output
  Handle(T, F)(G)
  -- Throw(T)(True)
  Exit

F() : ()
  Throw(T)(True)

G(x : Bool)(k : () ⟶ ()) : ()
  Unreachable

-- sum List⟦a⟧
--   Nil
--   Cons(a, List⟦a⟧)
